name: Hello World Test
on:
  push:
  workflow_dispatch:

jobs:
  hello_job:
    runs-on: ubuntu-latest
    steps:
        - name: Say Hello
          run: echo "Hello, GitHub Actions!"
        - name: Check Python Version
          run: python --version
